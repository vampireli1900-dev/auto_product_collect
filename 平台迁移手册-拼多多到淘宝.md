# 电商商品自动化采集系统 — 平台迁移手册

## 从拼多多到淘宝的开发经验复用指南

---

> **文档目的**：本文档将拼多多采集系统的开发经验系统化梳理，标注每个模块的平台耦合度，给出淘宝平台的改造方案，使开发者能够以最小成本完成平台迁移。

---

## 目录

1. [系统架构总览](#1-系统架构总览)
2. [模块平台耦合度分析](#2-模块平台耦合度分析)
3. [可直接复用的模块](#3-可直接复用的模块)
4. [需要适配改造的模块](#4-需要适配改造的模块)
5. [YOLO 模型训练迁移](#5-yolo-模型训练迁移)
6. [淘宝平台改造清单](#6-淘宝平台改造清单)
7. [关键设计模式与经验](#7-关键设计模式与经验)
8. [踩坑记录与最佳实践](#8-踩坑记录与最佳实践)

---

## 1. 系统架构总览

### 1.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    控制层 (dashboard.py)                  │
│   Streamlit Web UI → 设备管理 / 任务调度 / 实时监控        │
├─────────────────────────────────────────────────────────┤
│                    任务调度层 (task_manager.py)            │
│   Excel 任务队列 → 文件锁并发控制 → 领取/完成/重试机制      │
├─────────────────────────────────────────────────────────┤
│                    采集引擎层 (main.py / 采集10.3.py)      │
│   搜索 → 列表识别 → 优先级排序 → 详情页 → 信息提取 → 校验   │
├─────────────────────────────────────────────────────────┤
│                    智能分析层                              │
│   product_validator.py (校验) │ sku_matcher.py (规格匹配)  │
│   spec_utils.py (规格提取)    │ YOLO 模型 (视觉识别)       │
├─────────────────────────────────────────────────────────┤
│                    设备驱动层                              │
│   uiautomator2 + adb → 触控 / 截图 / XML 层级获取         │
└─────────────────────────────────────────────────────────┘
```

### 1.2 数据流

```
任务Excel ──→ TaskManager.claim_next() ──→ 采集引擎 ──→ 商品校验
                                                   │
                   ┌───────────────────────────────┘
                   ▼
            列表页截图 ──→ YOLO检测标签 ──→ 优先级排序
                   │
                   ▼
            点击商品 ──→ 详情页 ──→ EasyOCR + XML解析
                   │                    │
                   ▼                    ▼
            SKU规格匹配            标题/价格/日期
                   │                    │
                   └────────┬───────────┘
                            ▼
                    validate_product()
                            │
                            ▼
                    汇总Excel + 调试Excel
```

### 1.3 核心设计原则

| 原则 | 说明 |
|------|------|
| **视觉优先于逻辑** | 用 YOLO 识别 UI 元素而非硬编码坐标，适应不同分辨率和版本 |
| **多层校验兜底** | 品牌→规格→浓度→套装→品名→单例规则，逐层过滤 |
| **优雅降级** | 百亿补贴 → 品牌 → 全球购 → 普通商品，逐级降级采集 |
| **状态机管理** | 未采集 → 采集中 → 已采集/待复核/未采集（带冷却），状态闭环 |
| **文件锁并发** | 多设备共享同一 Excel 任务文件，OS 级排他锁保证安全 |

---

## 2. 模块平台耦合度分析

每个模块按耦合度分为三级：

| 标记 | 含义 |
|------|------|
| 🟢 **通用** | 零耦合，直接复用 |
| 🟡 **轻度耦合** | 少量平台相关常量/坐标，改几行即可 |
| 🔴 **重度耦合** | 深度依赖拼多多 UI 结构，需要重写核心逻辑 |

### 2.1 耦合度总表

| 文件 | 耦合度 | 可复用比例 | 改造工作量 |
|------|--------|-----------|-----------|
| `spec_utils.py` | 🟢 通用 | 100% | 无需改造 |
| `product_validator.py` | 🟢 通用 | ~95% | 仅需补充淘宝品牌库 |
| `task_manager.py` | 🟢 通用 | 100% | 无需改造 |
| `dashboard.py` | 🟡 轻度 | ~90% | 改 PDD 包名和重启命令 |
| `main.py` | 🔴 重度 | ~30% | 重写搜索/识别/提取核心逻辑 |
| `采集10.3.py` | 🔴 重度 | ~25% | 同上 + SKU 选择 UI 适配 |
| `sku_matcher.py` | 🔴 重度 | ~40% | 规格点击逻辑重写，提取逻辑复用 |
| YOLO 模型权重 | 🔴 重度 | 0% | 需要针对淘宝重新采集数据并训练 |

---

## 3. 可直接复用的模块

### 3.1 `task_manager.py` — 任务管理器（🟢 100% 复用）

**设计精华**：

```
ExcelTaskManager
├── _FileLock        # OS 级排他锁（O_CREAT | O_EXCL），简单可靠
├── claim_next()     # 领取策略：优先低重试次数，支持超时回收
├── finish()         # 完成回写：支持已采集/待复核/未采集三种终态
└── get_summary()    # 统计摘要：total/done/review/todo/成功率
```

**关键技术点**：

1. **文件锁而非数据库**：对于几十到几百条任务的场景，Excel + 文件锁比 SQLite 更直观，非技术人员也能直接编辑任务文件
2. **超时回收机制**：`COLLECT_RECLAIM_SECONDS`（默认 900s）自动回收卡在"采集中"状态的任务，防止进程崩溃导致任务永久丢失
3. **冷却时间（Cooldown）**：失败回写时设 `下次可重试时间 = now + 120s`，避免多设备同时抢同一失败任务
4. **重试次数优先**：`sort_values(by=["重试次数", "序号"])` 确保顽固任务不会阻塞整个队列

**关键代码结构**（可直接复制到淘宝项目）：

```python
class ExcelTaskManager:
    def __init__(self, excel_path: str):
        self.excel_path = excel_path
        self.lock_path = f"{excel_path}.lock"

    # _FileLock、_read_df、_ensure_columns、claim_next、finish、get_summary
    # 以上方法完全不涉及平台逻辑，可直接复用
```

**淘宝适配**：无需任何改动，仅需确保 Excel 列名一致。

---

### 3.2 `spec_utils.py` — 规格提取工具（🟢 100% 复用）

**核心能力**：

| 函数 | 功能 | 关键技术 |
|------|------|---------|
| `brand_lib` | 76 个美妆品牌字典，含中/英/法别名 | 最长匹配优先 |
| `extract_specs(text)` | 提取容量 + 色号 + 包装数量 | 多阶段正则 + 上下文过滤 |
| `extract_concentration(text)` | 提取香精浓度（EDP/EDT/浓香/淡香） | 模式匹配 |
| `extract_simple_pack(text)` | 检测简装标识 | 关键词匹配 |
| `concentration_match(a, b)` | 浓度一致性判断 | 同组匹配逻辑 |

**`extract_specs` 的设计思路（值得在新平台复用）**：

```
输入文本
  │
  ├─→ 容量提取：\d+ml/g/l/oz + 范围格式(30-50ml)
  │     └─→ 转换为数值集合
  │
  ├─→ 包装数量：*2, x2, 2支装, 两支装
  │     └─→ 数字集合（可能与容量合并）
  │
  ├─→ 色号提取（6 种模式级联）：
  │     ├── #数字色号：#1C1
  │     ├── 后置#：1N2#
  │     ├── 紧贴"色"/"号"：B20色
  │     ├── 字母+数字：L1, N2, LC1, NC20
  │     ├── 纯数字 2-4 位（排除年份/容量/营销数字）
  │     └── 数字+字母：8B, 02N
  │
  └─→ 过滤：去品牌别名、去容量单位、去营销数字
```

**淘宝适配**：美妆品牌库和规格提取逻辑完全通用，无需改动。如果是其他品类（如电子产品），需扩充品牌库和规格模式。

---

### 3.3 `product_validator.py` — 商品校验引擎（🟢 95% 复用）

**校验流水线**：

```
搜索词 ──────────────────→ 商品标题
  │                           │
  ├─ match_brand()      ├─ match_brand()
  ├─ extract_specs()    ├─ extract_specs()
  ├─ extract_conc()     ├─ extract_conc()
  ├─ extract_simple()   ├─ extract_simple()
  ├─ has_set_indicator()├─ has_set_indicator()
  └─ clean_title()      └─ clean_title()
        │                     │
        └──────┬──────────────┘
               ▼
    ┌──────────────────────┐
    │  逐维度校验           │
    │  ├─ brand_ok          │
    │  ├─ spec_ok (容量+色号)│
    │  ├─ conc_ok           │
    │  ├─ simple_ok         │
    │  ├─ set_ok            │
    │  └─ name_ok           │
    └──────────────────────┘
               │
               ▼
    ┌──────────────────────┐
    │  apply_manual_overrides │  ← 单例规则
    └──────────────────────┘
               │
               ▼
         final = all(oks)
```

**品名匹配的三级策略**：

```
优先级 1: word_bag_ratio() ≥ 30%           ← 词袋模型（主要）
    │     （中文 2-gram 滑动窗口 + normalize_token 同义词归一化）
    │
优先级 2: fuzzy_contains_no_stop()         ← 模糊包含（去虚词）
    │     （去除"的之了·・-"等虚词后做子序列匹配）
    │
优先级 3: fuzzy_contains()                 ← 模糊包含（保留虚词）
    │
优先级 4: LCS 子序列 ratio ≥ 30%           ← 兜底策略
```

**核心创新点**：

1. **同义词归一化** (`normalize_token`)：洗面奶→洁面，防晒乳→防晒，乳液→乳，肌底液→精华 等
2. **中文滑动窗口分词**：不需要依赖 jieba 等分词库，2-4 字滑动窗口覆盖所有可能词组
3. **核心品类词救急**：匹配率在 20%-30% 但核心品类词（散粉/气垫/唇膏等）命中时额外放行
4. **单例规则** (`apply_manual_overrides`)：为无法用通用逻辑处理的边缘 case 提供硬编码覆盖

**淘宝适配**：

- `brand_lib`：可扩充淘宝常见品牌（国货品牌如完美日记、花西子、珀莱雅等）
- `normalize_token` 同义词表：可补充淘宝平台常见表述
- `apply_manual_overrides`：清空，在淘宝上重新积累
- 其余逻辑 100% 复用

---

## 4. 需要适配改造的模块

### 4.1 `main.py` — 核心采集引擎（🔴 需重写）

#### 4.1.1 平台相关常量

```python
# ========== 拼多多 ==========
PACKAGE_NAME = "com.xunmeng.pinduoduo"
MAIN_ACTIVITY = "com.xunmeng.pinduoduo.ui.activity.MainFrameActivity"
SEARCH_EDIT_CLASS = "android.widget.EditText"
SEARCH_BTN_TEXT = "搜索"

# ========== 淘宝（需改为） ==========
PACKAGE_NAME = "com.taobao.taobao"
MAIN_ACTIVITY = "com.taobao.tao.homepage.MainActivity3"  # 待验证
SEARCH_EDIT_CLASS = "android.widget.EditText"             # 通常不变
SEARCH_BTN_TEXT = "搜索"                                   # 待验证
```

#### 4.1.2 各函数改造指南

##### `go_to_pinduoduo_home()` → `go_to_taobao_home()`

**拼多多实现**：
```python
subprocess.run(["adb", "-s", device_id, "shell", "am", "start",
    "-S", "-n",
    "com.xunmeng.pinduoduo/com.xunmeng.pinduoduo.ui.activity.MainFrameActivity"])
```

**淘宝改造**：
```python
def go_to_taobao_home(device_id: str) -> None:
    """强制返回淘宝首页"""
    # 方案1：通过 activity 启动（需要先确认淘宝主页 Activity）
    subprocess.run(["adb", "-s", device_id, "shell", "am", "start",
        "-n", "com.taobao.taobao/com.taobao.tao.homepage.MainActivity3"])
    
    # 方案2：如果 Activity 找不到，用 monkey 命令
    # subprocess.run(["adb", "-s", device_id, "shell",
    #     "monkey -p com.taobao.taobao -c android.intent.category.LAUNCHER 1"])
    
    # 方案3：force-stop 后重新启动（兜底）
    # subprocess.run(["adb", "-s", device_id, "shell",
    #     "am force-stop com.taobao.taobao"])
    # subprocess.run(["adb", "-s", device_id, "shell",
    #     "monkey -p com.taobao.taobao 1"])
```

> **⚠️ 踩坑提醒**：淘宝的 Activity 名称可能随版本变化，建议先用 `adb shell dumpsys package com.taobao.taobao | grep -A5 MAIN` 获取当前版本的入口 Activity。

##### `search_product()` → 搜索流程改造

**拼多多实现要点**：
1. 从屏幕中间向下拖拽（返回列表顶部）
2. 点击搜索框（约为屏幕高度 200/2400 比例位置）
3. 找到 `android.widget.EditText` 输入关键词
4. 点击文本为"搜索"的 `TextView` 或按 Enter

**淘宝的关键差异**：
- 搜索框位置不同，需要重新适配坐标比例
- 淘宝搜索按钮可能 text 不同（"搜索" / "Search" / 放大镜图标）
- 淘宝可能在搜索框获得焦点后自动弹出推荐，需要先清空再输入
- 淘宝可能有搜索历史浮层需要处理

**改造建议**：

```python
def search_product_taobao(d, keyword: str, index_num) -> None:
    """淘宝搜索 - 需重新适配"""
    print(f"\n搜索：{keyword}  序号：{index_num}")
    width, height = d.window_size()
    
    # 1. 回到顶部
    d.drag(width // 2, height // 2, width // 2, height // 2 + 400)
    time.sleep(1)
    
    # 2. 定位搜索框（淘宝搜索框通常在顶部）
    #    先用 uiautomator2 的 watcher 或 inspector 确定精确位置
    #    备选方案：通过 resourceId 或 content-desc 定位
    search_box = d(className="android.widget.EditText")
    if not search_box.exists(timeout=3):
        # 淘宝可能需要点击搜索入口
        d.click(width * 0.9, height * 0.08)  # 右上角搜索图标
        time.sleep(0.5)
    
    # 3. 输入关键词
    search_box = d(className="android.widget.EditText")
    search_box.clear_text()
    search_box.set_text(keyword)
    time.sleep(0.5)
    
    # 4. 触发搜索（淘宝可能是键盘上的搜索键）
    d.press("search")  # 或 d.press("enter")
    time.sleep(3)
```

##### `scan_list_products()` → YOLO 标签识别改造

**拼多多**：YOLO 识别三种标签：
- class 0 = `baiyi`（百亿补贴，红底白字）
- class 1 = `brand`（品牌黑标）
- class 2 = `global`（全球购）

**淘宝需识别**：
- 天猫标识（"天猫" 角标，通常灰色底）
- 品牌标识（品牌店铺标签）
- iFashion / 酷动城 等特色标签
- 金牌卖家
- 广告标识（"广告"/"hot"）

**改造方案**：

```python
# 淘宝标签枚举（需重新标注训练）
TAOBAO_LABELS = {
    0: "tmall",       # 天猫
    1: "brand",       # 品牌
    2: "gold_seller", # 金牌卖家
    3: "ifashion",    # iFashion
    4: "ad",          # 广告
}

# YOLO 模型加载
model = YOLO("runs/detect/taobao_label_train/weights/best.pt")
```

> **📌 模型训练见 [第5章](#5-yolo-模型训练迁移)**

##### `is_subsidy_product()` → 页面标识检测

**拼多多**：检查 XML 层级中是否包含"百亿补贴"或"官方补贴"

**淘宝**：根据业务需求改为：
- 是否天猫店：检查是否有"天猫"标识
- 是否聚划算：检查是否有"聚划算"标签
- 是否品牌直营：检查"品牌"相关 text

```python
def is_tmall_product(d) -> bool:
    """判断是否天猫商品"""
    hierarchy = d.dump_hierarchy()
    return "天猫" in hierarchy

def is_jhs_product(d) -> bool:
    """判断是否聚划算商品"""
    hierarchy = d.dump_hierarchy()
    return "聚划算" in hierarchy
```

##### `extract_product_info()` → 价格提取改造

**拼多多价格特征**：
- XML `content-desc` 中包含 `¥` 符号
- 存在原价（划线价）和现价（拼单价）
- 干扰信息：`单独购买`、`X人团`、`已抢X件`

**淘宝价格特征**：
- 价格通常显示为 `¥XXX.XX`
- 淘宝有"券后价"、"活动价"、"原价" 多种价格
- 可能显示为"到手价"
- 干扰信息："券后"、"满减"、"预售"

**改造建议**：

```python
def extract_product_info_taobao(xml_content: str, search_word: str) -> dict:
    """淘宝详情页信息提取"""
    # 1. 标题提取：复用 content-desc 遍历逻辑
    #    黑名单需调整（淘宝特有的噪声文本）
    blacklist = [
        "电池", "状态栏", "电量", "WLAN", "信号",
        "返回", "分享", "店铺", "客服", "购物车", "收藏",
        "¥", "￥", "券后", "到手价", "已售", "评价",
        "淘宝", "天猫", "我的淘宝", "消息", "购物车",
        # ... 淘宝特有噪声
    ]
    
    # 2. 价格提取规则需重新设计
    #    淘宝的价格可能在 text 属性而非 content-desc
    price_desc_list = []
    for desc in re.findall(r'(?:text|content-desc)="([^"]*)"', xml_content):
        if "¥" in desc:
            price_desc_list.append(desc)
    
    #    淘宝特有的过滤规则：
    #    - "券后" 后的价格是优惠后价格
    #    - "到手价" 可能是计算后的价格
    #    - 快递费、运费等需排除
    
    # 3. 淘宝可能没有"原价"和"现价"的明确区分
    #    改为采集：当前价 + 券后价（如有）
    
    return {
        "title": "...",
        "current_price": "...",
        "coupon_price": "...",  # 新增：券后价
    }
```

##### `find_and_click_detail()` → 详情页入口改造

**拼多多**：YOLO 检测"商品详情"按钮 → EasyOCR 验证 → 点击

**淘宝**：详情页通常在商品页面**下滑**即可看到，不一定有独立按钮

**改造方案**：

```python
def find_and_click_detail_taobao(d, max_scroll: int = 5) -> bool:
    """淘宝详情页定位 - 方案选择"""
    
    # 方案A：YOLO 检测（如果有明显的"详情"/"参数"标签）
    # 同拼多多逻辑，但检测目标改为淘宝的详情入口
    
    # 方案B：直接滑动到详情区域
    # 淘宝商品页下滑一段距离后就是详情描述
    for _ in range(max_scroll):
        img = d.screenshot(format="opencv")
        # YOLO 检测详情页特征（如"产品参数"/"商品详情"等文字）
        # ...
        d.drag(0.5w, 0.75h, 0.5w, 0.25h)
    
    # 方案C：点击"详情"Tab（如果有）
    # 淘宝商品页可能有"商品"/"详情"/"评价"三个Tab
    detail_tab = d(text="详情")
    if detail_tab.exists:
        detail_tab.click()
    
    return False
```

##### `get_date_with_retry()` → 日期提取改造

**拼多多**：正则匹配 `text="(\d{4}-\d{1,2}-\d{1,2})"`

**淘宝**：日期格式可能不同
- 可能显示为"生产日期：2026-07"
- 可能显示为"有效期至：2027-06-30"
- 有的品类（电子产品）没有生产日期概念

```python
def get_date_taobao(d) -> str:
    """淘宝日期提取 - 多格式兼容"""
    hierarchy = d.dump_hierarchy()
    
    # 尝试多种日期格式
    patterns = [
        r'(?:生产日期|有效期|保质期|到期)[：:]\s*(\d{4}[年/-]\d{1,2}[月/-]\d{1,2}[日]?)',
        r'text="(\d{4}-\d{1,2}-\d{1,2})"',
        r'text="(\d{4}\.\d{1,2}\.\d{1,2})"',
        r'text="(\d{4}年\d{1,2}月\d{1,2}日)"',
    ]
    for pat in patterns:
        m = re.search(pat, hierarchy)
        if m:
            return m.group(1)
    return ""
```

##### 优先级排序改造

**拼多多**：百亿补贴(4) > 品牌(2) > 全球购(1) > 普通(0)

**淘宝**：天猫品牌(3) > 金牌卖家(2) > 普通天猫(1) > 淘宝C店(0)

```python
def get_priority_taobao(tags: set) -> int:
    """淘宝商品优先级"""
    if "tmall" in tags and "brand" in tags:
        return 3
    if "gold_seller" in tags:
        return 2
    if "tmall" in tags:
        return 1
    return 0
```

---

### 4.2 `sku_matcher.py` — SKU 规格匹配（🔴 需重写）

#### 可复用部分（~40%）

| 函数 | 复用性 | 说明 |
|------|--------|------|
| `get_sku_identifiers()` | 🟢 完全复用 | 从搜索词提取色号/容量/英文名的逻辑与平台无关 |
| `is_identifier_match()` | 🟢 完全复用 | 字母数字组合匹配逻辑通用 |
| `_get_brand_aliases_lower()` | 🟢 完全复用 | 品牌别名缓存，通用 |

#### 需重写部分（~60%）

| 函数 | 原因 |
|------|------|
| `_click_sku_by_identifier()` | 淘宝 SKU 选择器 UI 完全不同（可能是弹窗/底部Sheet/列表） |
| `_select_style_if_needed()` | 淘宝的"款式"按钮位置、样式不同 |
| `extract_sku_price_with_id()` | 淘宝价格节点的 XML 结构和拼多多不同 |
| `get_sku_price_auto()` | 整体交互流程需重新设计 |
| `_count_capacity_occurrences()` | XML 搜索模式是平台相关的 |

**淘宝 SKU 交互特点**（需实际操作淘宝确认）：
- SKU 选择区域通常在商品标题下方，以按钮组形式展示
- 可能分"规格"（容量/颜色）和"套餐类型"两级
- 价格随 SKU 切换实时更新，可能在页面顶部显示
- 部分商品 SKU 是下拉列表而非按钮组
- 可能出现"缺货"灰色按钮状态

**改造思路**：

```python
def select_sku_taobao(d, search_word: str) -> dict:
    """淘宝 SKU 选择"""
    identifiers = get_sku_identifiers(search_word)  # 复用
    
    # 获取当前页面的 SKU 选项列表
    hierarchy = d.dump_hierarchy()
    
    # 淘宝 SKU 按钮的常见特征（需实际调试确认）：
    # - className 可能是 android.widget.TextView
    # - 可能在一个 RecyclerView 或 ViewGroup 中
    # - 已选中的 SKU 可能有特殊样式（如边框高亮）
    
    # 匹配并点击正确的 SKU
    for ident in identifiers:
        elem = d(textContains=ident)
        if elem.exists:
            elem.click()
            time.sleep(0.5)
            break
    
    # 获取选择后的价格
    time.sleep(0.3)
    hierarchy = d.dump_hierarchy()
    price = extract_price_from_sku_page(hierarchy)
    
    return {"spec": ident, "price": price}
```

---

### 4.3 `dashboard.py` — 控制面板（🟡 轻度改造）

**需改内容**：

| 位置 | 改动 |
|------|------|
| PACKAGE_NAME 引用 | 字符串替换 `com.xunmeng.pinduoduo` → `com.taobao.taobao` |
| 重启 App 按钮 | `adb shell am force-stop` + `am start` 的 activity 名 |
| `go_to_pinduoduo_home` 导入 | 改为 `go_to_taobao_home` |
| 页面标题/说明文字 | "拼多多" → "淘宝" |
| 默认任务文件路径 | 可保留或改名 |

除此之外，看板的整体架构（Streamlit 页面布局、设备管理、worker 线程模型、遥测信号系统）**100% 复用**。

---

### 4.4 `采集10.3.py` — 独立采集脚本（🔴 需重写）

此文件是 `main.py` + `sku_matcher.py` 的整合版，包含：
- 商品标签分类（百亿补贴/品牌黑标/全球购/普通）→ 改为淘宝标签
- SKU 规格匹配和规格价格采集 → 重写
- 额外的 Excel 列（匹配规格、规格价格、商品标签）

改造策略：建议基于改造后的 `main.py` 同步修改，而非独立适配。

---

## 5. YOLO 模型训练迁移

### 5.1 现有模型清单与淘宝对应

| 拼多多模型 | 用途 | 淘宝对应 | 训练难度 |
|-----------|------|---------|---------|
| `pdd_logo_train-2` | 列表页商品标签（百亿/品牌/全球购） | 淘宝标签（天猫/品牌/金牌卖家/iFashion/广告） | ⭐⭐⭐ |
| `subsidy_train` | 百亿补贴标识检测 | 聚划算/百亿补贴标识 | ⭐⭐ |
| `product_detail_train` | "商品详情"按钮检测 | 详情入口/Tab按钮 | ⭐⭐ |

### 5.2 训练流程（标准化流程，适用于任何平台）

```
第1步：数据采集
├── 用 uiautomator2 自动截取淘宝列表页（1000+ 张）
├── 覆盖不同品类、不同分辨率、不同版本
└── 确保标签多样性（每种标签≥200个样本）

第2步：数据标注
├── 工具：LabelImg / LabelStudio / CVAT
├── 格式：YOLO 格式（class x_center y_center width height 归一化）
├── 标签定义：
│   ├── 0: tmall (天猫)
│   ├── 1: brand (品牌)
│   ├── 2: gold_seller (金牌卖家)
│   ├── 3: ifashion (iFashion)
│   └── 4: ad (广告)
└── 标注量：每个标签建议 300-500 个样本

第3步：训练配置
├── 基模型：yolov8n.pt（已有）/ yolov8s.pt（更高精度）
├── 参数参考：
│   ├── epochs: 100-200
│   ├── imgsz: 640
│   ├── batch: 16
│   ├── lr0: 0.01
│   └── augment: True (hsv_h, hsv_s, hsv_v, flipud)
└── 设备：GPU 推荐（MPS/CPU 也可，但较慢）

第4步：模型导出与部署
├── 输出：runs/detect/taobao_label_train/weights/best.pt
└── 使用：model = YOLO("runs/detect/taobao_label_train/weights/best.pt")
```

### 5.3 训练命令模板

```bash
# 训练列表页标签模型
yolo detect train \
  data=taobao_labels.yaml \
  model=yolov8n.pt \
  epochs=150 \
  imgsz=640 \
  batch=16 \
  name=taobao_label_train

# taobao_labels.yaml 内容：
# path: /path/to/dataset
# train: images/train
# val: images/val
# nc: 5
# names: ['tmall', 'brand', 'gold_seller', 'ifashion', 'ad']
```

### 5.4 EasyOCR 与 YOLO 的协作模式

拼多多项目中的一个重要设计模式是 **YOLO + EasyOCR 组合验证**：

```
YOLO 粗定位 → EasyOCR 细验证
     │               │
     │               └─ 文字识别确认内容
     └─ 视觉特征定位候选区域
```

例如`find_and_click_detail()`：
1. YOLO 找到所有 class=0 的候选框
2. 对每个框做 EasyOCR 文字识别
3. 确认文字内容是否为"商品详情"
4. 只有匹配才点击

这个模式在淘宝上同样适用，只需替换 YOLO 模型和 OCR 验证的关键词。

---

## 6. 淘宝平台改造清单

### 6.1 改造步骤总览

```
阶段1：信息收集（1-2天）
├── 安装淘宝 App，用 uiautomator2 inspector 分析 UI 结构
├── 确定包名、Activity 名
├── 分析搜索流程的 XML 层级
├── 分析商品列表页的 XML 层级（标签位置、样式）
├── 分析商品详情页的结构（SKU 区域、价格显示、详情入口）
└── 截图 100+ 张用于后续 YOLO 训练

阶段2：基础框架搭建（1-2天）
├── 复制 task_manager.py、spec_utils.py、product_validator.py（复用）
├── 创建 taobao_main.py（改造 main.py）
├── 修改 go_to_home / search / scroll 等基础操作
└── 用 1-2 个商品做端到端手动测试

阶段3：YOLO 模型训练（2-3天）
├── 标注列表页标签数据
├── 训练淘宝标签检测模型
├── 训练详情入口检测模型（如需要）
└── 验证模型精度

阶段4：核心采集逻辑（2-3天）
├── 实现列表页标签识别与优先级排序
├── 实现详情页导航与信息提取
├── 实现 SKU 规格匹配
├── 实现价格提取（适配淘宝格式）
└── 端到端测试

阶段5：校验与调优（1-2天）
├── 调整 product_validator 单例规则（清空后重积累）
├── 补充淘宝特有品类品牌库
├── 错误处理与容错优化
└── 跑一批测试数据，对比人工校验结果

阶段6：看板适配（0.5天）
├── 修改 dashboard.py 中的 PDD 常量
└── 测试多设备并行

阶段7：上线与迭代
├── 小批量试运行
├── 根据失败记录调整规则
└── 逐步扩大采集规模
```

### 6.2 详细改造清单

| 序号 | 改造项 | 文件 | 工作量 | 优先级 |
|------|--------|------|--------|--------|
| 1 | 包名 + Activity | main.py | 5 min | P0 |
| 2 | 搜索流程 | main.py | 2 h | P0 |
| 3 | 列表页标签检测 YOLO 模型 | 新文件 | 2-3 天 | P0 |
| 4 | 优先级排序逻辑 | main.py | 1 h | P0 |
| 5 | 详情页进入方式 | main.py | 2 h | P0 |
| 6 | 价格提取正则 | main.py | 3 h | P0 |
| 7 | SKU 选择交互 | sku_matcher.py | 1 天 | P1 |
| 8 | 日期提取 | main.py | 1 h | P1 |
| 9 | 黑名单噪声词更新 | main.py | 1 h | P1 |
| 10 | 品牌库扩充（淘宝国货） | spec_utils.py | 2 h | P2 |
| 11 | 单例规则重置 | product_validator.py | 持续 | P2 |
| 12 | 看板文字替换 | dashboard.py | 30 min | P1 |
| 13 | 测试 + 调试 | 全部 | 2 天 | P0 |

---

## 7. 关键设计模式与经验

### 7.1 优先级降级策略

```python
# 核心思想：从高价值到低价值逐级尝试，任一级通过即停止
priority_order = sorted(set(get_priority(p["tags"]) for p in sorted_prods), reverse=True)
any_passed = False

for prio in priority_order:
    candidates = [p for p in sorted_prods if get_priority(p["tags"]) == prio]
    for p in candidates:
        res = collect_single_product(...)
        if res["passed"]:
            any_passed = True
    if any_passed:
        break  # 高优先级通过，不再降级
```

**设计考量**：

- 百亿补贴商品数据价值最高，优先采集
- 避免降级到低质量商品（如全部是全球购）
- 每级最多采集 2-3 个商品，防止无限循环
- 全球购特殊处理：回到顶部重新识别，不触发自动下滑

**淘宝适配建议**：天猫品牌旗舰店 > 金牌卖家 > 普通天猫 > C店

### 7.2 三级防重复机制

```
第1级：采集前
├── task_manager claim_next 加锁领取
└── 采集中状态避免多设备领同一任务

第2级：采集后写入
├── drop_duplicates(subset=["序号", "货品名称", "现价"])
└── 去重写入汇总表

第3级：调试记录
├── drop_duplicates(subset=["搜索关键词", "提取到的商品标题", "校验时间"])
└── 调试记录也去重
```

### 7.3 程序崩溃恢复机制

```python
# task_manager.py 中的超时回收
reclaim_after_s = float(os.environ.get("COLLECT_RECLAIM_SECONDS", "900"))
doing_mask = df["状态"] == "采集中"
for ridx in df[doing_mask].index.tolist():
    started_ts = _parse_ts(df.at[ridx, "开始时间"])
    if now_ts - started_ts >= reclaim_after_s:
        df.at[ridx, "状态"] = "未采集"     # 回退
        df.at[ridx, "领取设备"] = ""
        df.at[ridx, "开始时间"] = ""
        df.at[ridx, "备注"] = "采集中超时回收"
```

### 7.4 失败冷却与重试递增

```python
# 失败后不立即重试，给 120s 冷却
if final_status == "未采集" and retry_inc:
    cooldown_s = 120
    df.at[row_idx, "下次可重试时间"] = _format_ts(time.time() + cooldown_s)

# 领取时检查冷却
next_ts = _parse_ts(candidates.at[ridx, "下次可重试时间"])
if next_ts is None or next_ts <= now_ts:
    allowed_rows.append(ridx)  # 冷却到期或首次尝试
```

### 7.5 坐标比例化设计

**反例（拼多多早期代码中的问题）**：
```python
d.drag(500, 1800, 500, 600, 0.25)  # 硬编码 1080x2400 的像素坐标
```

**正例（改进后的代码）**：
```python
width, height = d.window_size()
d.drag(
    int(width * 0.46),   # 500/1080 ≈ 0.46
    int(height * 0.75),  # 1800/2400 = 0.75
    int(width * 0.46),
    int(height * 0.25),  # 600/2400 = 0.25
    0.25
)
```

> **⚠️ 重要教训**：所有坐标操作必须比例化。手机分辨率从 1080x2400 到 1440x3200 不等，硬编码坐标在不同设备上会点偏。

### 7.6 信号/遥测系统（Signal/Telemetry）

用于连接采集引擎和看板的实时通信：

```python
def _emit(signal, phase, detail="", extra=None):
    """更新状态信号，看板实时读取"""
    signal["阶段"] = phase
    signal["阶段说明"] = detail
    signal["更新时间"] = time.strftime("%Y-%m-%d %H:%M:%S")

def _trace(signal, line):
    """追加采集轨迹，最多保留 _TRACE_CAP 行"""
    buf = signal.setdefault("采集轨迹", [])
    buf.append(line)
    if len(buf) > _TRACE_CAP:
        del buf[:len(buf) - _TRACE_CAP]
```

信号通过 `dev_ref["_遥测"]` 字典在 worker 线程和 Streamlit UI 之间共享（同一进程内），无需额外通信机制。

### 7.7 单任务超时保护

```python
def _run_one_collect(device_id, index_num, keyword, dev_ref, timeout_s):
    boxed = {}
    def runner():
        boxed["result"] = collect_one_task(...)
    
    th = threading.Thread(target=runner, daemon=True)
    th.start()
    th.join(timeout=timeout_s)
    if th.is_alive():
        return {"ok": False, "remark": f"单任务超时 {timeout_s} 秒"}, "单任务超时"
```

> **设计考量**：Python 线程无法强制 kill，所以超时后只是标记失败并返回，原线程继续运行（daemon 线程会在主进程退出时自动清理）。对于采集任务，240s 的超时足够覆盖正常流程。

---

## 8. 踩坑记录与最佳实践

### 8.1 uiautomator2 常见坑

| 坑 | 现象 | 解决方案 |
|----|------|---------|
| `wait_idle` 永不返回 | 拼多多列表页有无限动画 | 用 `try/except` 包裹，设 5s timeout |
| XML dump 超时 | 页面元素过多 | 不依赖完整 dump，用正则匹配关键 text |
| 截图过大 | 高分辨率设备截图 5MB+ | 用 `format="opencv"` 直接返回 numpy 数组 |
| 控件 stale | 页面刷新后之前获取的控件失效 | 每次操作前重新获取控件 |
| `textContains` 大小写 | Android 控件 text 大小写不确定 | 使用 XPath `translate()` 做大小写不敏感匹配 |

### 8.2 YOLO 检测精度优化

1. **conf 阈值**：列表页用 `conf=0.2`（宁可多检不漏检），详情按钮用 `conf=0.75`（需要高精度）
2. **推理锁**：YOLO 推理不是线程安全的，使用 `_infer_lock` 保护
3. **模型预加载**：首次加载 YOLO 模型需要 2-3s，用 `_get_runtime()` 做全局单例懒加载

### 8.3 EasyOCR 使用建议

1. **GPU vs CPU**：此项目用 `gpu=False`，因为在手机上跑不需要 GPU 加速，且 MPS/CUDA 初始化有额外开销
2. **文字方向**：拼多多商品标题是横向的，不需要旋转检测
3. **只做验证不做全量 OCR**：先 YOLO 定位区域，再对裁剪图做 OCR，大幅减少计算量

### 8.4 XML 层级解析技巧

拼多多的商品列表页 XML 层级非常深（50+ 层），直接解析 DOM 树效率低。采用的策略：

```python
# 不用 DOM 解析，直接用正则匹配关键字段
desc_list = re.findall(r'content-desc="([^"]+)"', xml_content)
```

**优点**：快、不依赖结构变化
**缺点**：无法获取控件位置/层级关系

对于需要位置信息的场景（如 SKU 点击），使用 `uiautomator2` 的 XPath API：
```python
xpath = f'//*[contains(@text, "{identifier}")]'
elem = d.xpath(xpath)
```

### 8.5 淘宝特有的注意事项

| 事项 | 说明 |
|------|------|
| **登录态** | 淘宝比拼多多更严格，可能需要保持登录。建议提前手动登录 |
| **风控** | 淘宝的反自动化检测可能更严格，操作间隔适当拉长 |
| **验证码** | 搜索频率过高可能触发滑块验证码 |
| **多设备** | 同一 IP 多设备同时采集可能触发风控 |
| **WebView** | 淘宝部分页面是 WebView 实现，uiautomator2 可能无法获取内部元素 |
| **版本更新** | 淘宝 UI 更新频繁，需要持续维护选择器 |
| **XPath 性能** | 淘宝页面层级可能比 PDD 更深，XPath 查询可能更慢 |

### 8.6 品名校验阈值调优

当前拼多多的词袋匹配阈值是 **30%**，这个值是反复调试的结果：

- 太高（50%）：很多实际匹配的商品被拒（假阴性多）
- 太低（15%）：大量不相关的商品被放行（假阳性多）
- 30% + 核心品类词救急：在准确率和召回率之间取得平衡

淘宝平台需要根据实际测试数据重新校准这个阈值。

### 8.7 品牌库维护策略

```python
brand_lib = {
    "赫莲娜": ["HR", "HelenaRubinstein", "Helena Rubinstein", "赫莲娜"],
    # 每个品牌一个标准名 + 多个别名
    # 别名包括：英文全名（去空格）、英文简写、中文昵称、常见错别字
}
```

**设计原则**：
1. 键是标准中文名，值列表是别名
2. 优先匹配最长的别名（`best_len` 机制），避免"MAC"误匹配到包含"mac"的英文词
3. 别名列表中放拼音/英文去空格版，因为 OCR 经常丢失空格
4. 常见错别字也加入别名（如"茵芙莎"/"茵芙纱"）

---

## 附录 A：逐函数改造对照表

> 基于 `main.py`、`sku_matcher.py`、`dashboard.py`、`采集10.3.py` 的逐行审查结果。

### A.1 `main.py` — 核心采集引擎

| 函数/区域 | 行号 | PDD 当前值 | 淘宝需改为 |
|-----------|------|-----------|-----------|
| `PACKAGE_NAME` | 40 | `com.xunmeng.pinduoduo` | `com.taobao.taobao` |
| `go_to_home()` Activity | 269 | `com.xunmeng.pinduoduo.ui.activity.MainFrameActivity` | 需用 `adb shell dumpsys package com.taobao.taobao \| grep -A5 MAIN` 获取 |
| `search_product()` 搜索框 Y 坐标 | 297 | `int(height * 200 / 2400)` | 淘宝搜索框位置不同，需重新标定 |
| `search_product()` 搜索按钮 | 308 | `d(text="搜索", className="android.widget.TextView")` | 淘宝可能是放大镜图标，text 可能为空，需用 XPath 或坐标兜底 |
| YOLO 列表页模型 | 89 | `pdd_logo_train-2/weights/best.pt` | 重新训练淘宝标签模型 |
| YOLO 详情按钮模型 | 90 | `product_detail_train/weights/best.pt` | 重新训练淘宝详情入口模型 |
| `scan_list_products()` 标签 | 340-345 | class 0=baiyi, 1=brand, 2=global | 淘宝标签：tmall/品牌/金牌卖家/iFashion/广告 |
| `get_priority()` | 366-373 | baiyi=4, brand=2, global=1 | 重新定义：天猫+品牌=3, 金牌卖家=2, 普通天猫=1 |
| `is_subsidy_product()` | 414 | `"百亿补贴"` 或 `"官方补贴"` | 改为 `"百亿补贴"` 或 `"聚划算"` 或 `"官方立减"`（淘宝用词） |
| `extract_product_info()` 黑名单 | 438-444 | 含 `"拼小圈"`, `"拼单价"`, `"大促价"`, `"已抢"`, `"假一赔十"`, `"直接成团"`, `"狂降"`, `"tronplayer_view"` | 替换为 `"购物车"`, `"加入购物车"`, `"领券"`, `"关注"`, `"猜你喜欢"`, `"收藏"`, `"推荐"`, `"已售"`, `"评价"` |
| `extract_product_info()` 价格清洗 | 486 | `"单独购买"` | 替换为 `"券后价"`, `"预估到手价"` |
| `extract_product_info()` 价格清洗 | 501 | `r"\d人团"` | 删除（淘宝无拼团） |
| `extract_product_info()` 价格清洗 | 509 | `r"降\d+\.?\d*"` | 替换为 `r"满\d+减\d+"`, `r"领券"` |
| `extract_product_info()` 价格清洗 | 513 | `r"\d{2}人想拼"` | 删除 |
| `find_and_click_detail()` 验证文本 | 584 | `"商品详情"` | 改为 `"详情"` 或 `"宝贝详情"` 或 `"图文详情"` |
| `find_and_click_detail()` 详情页判断 | 589 | `"生产日期"` | 改为 `"产品参数"` 或 `"商品参数"` |
| `get_date_with_retry()` | 607 | `r'text="(\d{4}-\d{1,2}-\d{1,2})"'` | 日期可能在 `content-desc` 而非 `text`，格式可能不同 |

### A.2 `采集10.3.py` — 独立采集脚本

| 函数/区域 | 行号 | PDD 当前值 | 淘宝需改为 |
|-----------|------|-----------|-----------|
| SKU 面板点击 | 567 | `d.click(int(width * 0.8), int(height * 0.96))` | 淘宝 SKU 入口位置不同，需重新定位 |
| 商品标签分类 | 528-535 | `"百亿补贴"`, `"品牌黑标"`, `"全球购"` | 改为淘宝标签体系 |
| `ensure_back_to_list()` 弹窗文本 | 670 | `"放弃优惠"`, `"继续退出"` | 淘宝退出弹窗文案不同（如有） |
| 搜索进店检测 | 197-199 | `"进店" in xml` | 淘宝搜索行为不同，可能无此场景 |

### A.3 `sku_matcher.py` — SKU 规格匹配

| 函数/区域 | 行号 | PDD 当前值 | 淘宝需改为 |
|-----------|------|-----------|-----------|
| `extract_sku_price_with_id()` 已选检测 | 165 | `re.search(r'..."已选(?:择)?..."')` | "已选"在淘宝也常见，大概率可用 |
| `extract_sku_price_with_id()` 价格提取 | 213 | `content-desc` 或 `text` 属性中的 `¥` 价格 | 淘宝 SKU 面板价格可能在 `text` 属性中，具体需调试 |
| `_select_style_if_needed()` 款式 XPath | 373 | `'//android.widget.TextView[contains(@text, "【")]'` | 淘宝款式按钮格式可能不同，可能是 `[` 或无括号 |
| `get_sku_price_auto()` 浓度黑名单 | 432 | `{'edp', 'edt', 'edc', 'parfum', ...}` | 🟢 **完全复用**（产品域知识，与平台无关） |
| `get_sku_identifiers()` | 25-127 | 全部 | 🟢 **完全复用**（纯文本提取，无平台依赖） |
| `is_identifier_match()` | 130-150 | 全部 | 🟢 **完全复用**（纯字符串匹配） |
| `is_valid_spec_text()` 地址过滤 | 264-267 | `['路', '号', '铺', '城', ...]` | 🟢 **完全复用**（通用中文地址过滤） |
| `is_valid_spec_text()` 噪声过滤 | 269 | `['快递', '邮费', '运费', ...]` | 🟢 **完全复用**（通用电商噪声） |

### A.4 `dashboard.py` — 控制面板

| 位置 | PDD 当前值 | 淘宝需改为 |
|------|-----------|-----------|
| 导入 (L26) | `from main import ... go_to_pinduoduo_home` | `from taobao_main import ... go_to_taobao_home` |
| 超时恢复 (L188) | `go_to_pinduoduo_home(device_id)` | `go_to_taobao_home(device_id)` |
| 重启按钮 (L609-621) | `"重启 PDD"`, `com.xunmeng.pinduoduo` | `"重启 TB"`, `com.taobao.taobao` |
| 日志 (L621) | `f"设备 {did} 已重启拼多多"` | `f"设备 {did} 已重启淘宝"` |
| 页面标题 (L244) | `"多机群控采集系统"` | 不变或改为 `"淘宝多机群控采集系统"` |
| **其余全部代码** | — | 🟢 **完全复用**（Streamlit UI、设备管理、worker 线程、遥测系统） |

### A.5 `task_manager.py` — 任务管理器

| 结论 | 说明 |
|------|------|
| 🟢 **100% 复用** | 零平台依赖。Excel 任务队列、文件锁、状态机、冷却机制全部通用 |

### A.6 `spec_utils.py` — 规格提取

| 结论 | 说明 |
|------|------|
| 🟢 **100% 复用** | 品牌库、规格提取、浓度匹配全为产品域知识，与电商平台无关 |

---

## 附录 B：淘宝 UI 探索清单

在开始写代码前，建议先用 `uiautomator2` 的 inspector 或 `weditor` 工具完成以下探索：

```
□ 淘宝首页结构
  ├── 搜索框的 resourceId / className / 坐标范围
  ├── 搜索按钮的 text / content-desc
  └── 首页 Activity 名称

□ 搜索结果页结构
  ├── 商品列表的容器类型（RecyclerView / ListView / WebView?）
  ├── 每个商品卡片的 XML 结构
  ├── 价格显示的 text/content-desc 格式
  ├── 商品标题的 text/content-desc 格式
  ├── 天猫/品牌/广告等标签的样式和位置
  └── 是否可通过 XML 层级区分标签

□ 商品详情页结构
  ├── 标题的 text/content-desc
  ├── 价格的 text/content-desc（注意是否有多种价格）
  ├── SKU 规格选择区域的结构
  ├── 已选 SKU 的标识文本
  ├── 详情描述的位置（是否需要滑动）
  ├── 生产日期/有效期位置
  └── 页面是否包含 WebView

□ 全局交互
  ├── 搜索建议弹窗（是否需要关闭）
  ├── 广告弹窗/新人优惠弹窗拦截
  ├── 权限弹窗处理
  └── 网络错误页面处理
```

## 附录 B：关键代码片段速查

### B.1 设备连接与 App 启动

```python
import uiautomator2 as u2

# 连接设备
d = u2.connect("device_serial")  # 或 u2.connect() 连第一个设备

# 启动 App
d.app_start("com.taobao.taobao")

# 获取屏幕尺寸
width, height = d.window_size()

# 截图（多种格式）
img_cv = d.screenshot(format="opencv")    # numpy 数组，给 YOLO
d.screenshot("file.jpg")                   # 保存到文件

# 获取 UI 层级
xml = d.dump_hierarchy()                   # XML 字符串
```

### B.2 YOLO 推理

```python
from ultralytics import YOLO

model = YOLO("path/to/best.pt")
results = model(img_cv, conf=0.2)

for r in results:
    for box in r.boxes:
        cls = int(box.cls)        # 类别 ID
        x1, y1, x2, y2 = map(int, box.xyxy[0])  # 边界框
        cx, cy = (x1 + x2) // 2, (y1 + y2) // 2  # 中心点
```

### B.3 EasyOCR 识别

```python
import easyocr

reader = easyocr.Reader(['ch_sim'], gpu=False)

# 对裁剪区域做 OCR
crop = img_cv[y1:y2, x1:x2]
results = reader.readtext(crop)
text = "".join(item[1] for item in results)
```

### B.4 Excel 安全写入（文件锁）

```python
# 完整实现在 task_manager.py
# 核心：用 os.open 的 O_CREAT | O_EXCL 实现原子锁
fd = os.open(lock_path, os.O_CREAT | os.O_EXCL | os.O_RDWR)
# ... 读写 Excel ...
os.close(fd)
os.unlink(lock_path)
```

---

## 附录 C：环境变量配置

项目支持通过环境变量灵活配置（设计良好，可复用）：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `ANDROID_SERIAL` | - | 目标设备序列号 |
| `COLLECT_PROJECT_DIR` | 脚本所在目录 | 项目根目录 |
| `COLLECT_TASK_XLSX` | `测试用例.xlsx` | 任务文件路径 |
| `COLLECT_SUMMARY_XLSX` | `商品采集汇总.xlsx` | 汇总输出路径 |
| `COLLECT_DEBUG_XLSX` | `商品校验调试记录.xlsx` | 调试记录路径 |
| `COLLECT_SEARCH_INTERVAL` | 40 | 任务间隔秒数 |
| `COLLECT_TRACE_MAX_LINES` | 800 | 采集轨迹最大行数 |
| `COLLECT_RECLAIM_SECONDS` | 900 | 采集中任务回收超时 |
| `COLLECT_TRACE_MIRROR_HEIGHT` | 520 | 看板镜像窗口高度 |
| `COLLECT_TRACE_PREVIEW_HEIGHT` | 320 | 看板预览窗口高度 |

---

> **文档版本**: v1.0  
> **最后更新**: 2026-07-29  
> **适用平台**: 拼多多 → 淘宝 迁移  
> **目标读者**: 已理解拼多多采集系统的开发者
