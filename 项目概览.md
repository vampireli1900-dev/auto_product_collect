项目概览：电商商品自动化采集系统（拼多多）
这是一个基于 Python + UIAutomator2 + YOLOv8 + EasyOCR + LLM 构建的拼多多商品数据自动化采集系统。

核心技术栈
模块	技术
手机自动化控制	uiautomator2 + adb
商品标签识别	YOLOv8（3个模型：品牌标签、百亿补贴标签、商品详情按钮）
OCR 文字提取	easyocr
商品校验	规则引擎 + 词袋匹配 + LCS 算法
语义匹配	GLM-4 / Qwen-4B（备用）
控制面板	Streamlit 多机群控看板
数据处理	pandas / openpyxl
项目结构

整合详情页采集/
├── main.py                    # 🔑 主程序入口（当前活跃版本）
├── 采集10.3.py                # 最新版采集脚本（新增SKU规格匹配+标签）
├── 采集10.2.py                # 上一版采集脚本
├── dashboard.py               # 🖥️ Streamlit 多机群控看板
├── run_collector.py           # 看板调用入口
├── task_manager.py            # Excel 任务管理器（文件锁、并发领取）
├── product_validator.py       # 🔍 商品校验引擎（品牌/规格/品名匹配）
├── spec_utils.py              # 规格提取工具（容量/色号/品牌库 76个品牌）
├── sku_matcher.py             # SKU 规格自动匹配（点击规格选价格）
├── yolo26n.pt                 # YOLOv8 模型文件（5.5MB）
├── runs/detect/               # 🧠 YOLO 训练权重
│   ├── pdd_logo_train-2/      # 列表页商品标签识别（百亿/品牌/全球购）
│   ├── subsidy_train/         # 百亿补贴检测
│   └── product_detail_train/  # 商品详情按钮检测
├── 商品采集汇总.xlsx          # 采集结果输出
├── 商品校验调试记录.xlsx      # 校验过程调试记录
├── 搜索名单.xlsx              # 任务输入文件
├── 复采名单.xlsx              # 重采任务名单
├── collector_logs.txt         # 运行日志
├── 模块开发/                  # 模块化工具集
│   ├── 录入V1.0.py            # 商品信息录入
│   ├── 校验V1.6.py            # 商品校验模块
│   ├── 色号V0.4稳定版.py      # 色号提取模块
│   └── 看板V0.1.py            # 早期看板原型
└── 往期采集版本/              # 历史版本备份
核心工作流程
搜索商品 → ADB 控制拼多多 App 搜索关键词
列表页识别 → YOLOv8 检测商品标签（百亿补贴/品牌/全球购），按优先级排序
优先采集策略：百亿补贴(4) > 品牌(2) > 全球购(1) > 普通(0)
进入详情页 → YOLOv8 定位"商品详情"按钮，自动点击
提取信息 → EasyOCR + XML 层级解析提取：标题、原价/现价、生产日期、是否补贴
商品校验（product_validator.py）：
品牌匹配（76个品牌库，中英文别名）
规格匹配（容量、色号、浓度、简装、套装）
品名匹配（词袋模型 30% 阈值 + LCS 兜底）
单例规则覆盖（特殊 case 处理）
SKU 规格匹配（sku_matcher.py）→ 自动点击对应规格并获取价格
写入 Excel → 结构化保存到 商品采集汇总.xlsx
多机群控看板 (dashboard.py)
基于 Streamlit 的 Web 控制台，支持：

总览：任务进度、设备状态、成功率
采集实况：实时控制台镜像（每台设备的输出流）
任务管理：查看/编辑任务列表
设备与调度：扫描 ADB 设备、启动/停止采集线程、重启 PDD
采集参数：每设备独立配置（间隔、休息阈值、超时等）
进度与异常：失败条目追踪、重复去重
运行模式
--standalone：单机循环领取模式（配合 ExcelTaskManager）
--device-id + --keyword + --index-num：单任务模式
--json：JSON 输出（供外部调用）
dashboard.py：多机群控 Web 看板
run_collector.py：看板调用入口（接收设备 ID 参数）
Git 最近动态（20 条提交）
主要围绕：校验规则优化、SKU 规格匹配、色号模块、录入模块、生产日期提取、品牌 debug 等方向持续迭代。最新提交是 fix:规格匹配失常+完善录入新增商品标签功能适配。

这是一个成熟的生产级自动化采集系统，具备完善的任务调度、异常恢复、多设备并行和数据分析能力。需要我深入分析哪个模块或帮你做什么改动吗？